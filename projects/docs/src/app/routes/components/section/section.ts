import { Component } from '@angular/core';
import { IUI } from '@insight/ui';

@Component({
  selector: 'ir-section',
  imports: [IUI],
  templateUrl: './section.html',
  styleUrl: './section.scss',
})
export class IRSection {}
