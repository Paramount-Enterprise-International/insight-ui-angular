import { Component } from '@angular/core';
import { IUI } from '@insight/ui';

@Component({
  selector: 'ir-padding',
  imports: [IUI],
  templateUrl: './padding.html',
  styleUrl: './padding.scss',
})
export class IRUtilitiesPadding {}
