import { Component } from '@angular/core';
import { IUI } from '../../../../ui';

@Component({
  selector: 'ir-radius',
  imports: [IUI],
  templateUrl: './radius.html',
  styleUrl: './radius.scss',
})
export class IRVariableRadius {}
