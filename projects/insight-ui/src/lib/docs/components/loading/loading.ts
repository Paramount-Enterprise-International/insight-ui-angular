import { Component } from '@angular/core';
import { IUI } from '../../../ui';

@Component({
  selector: 'ir-loading',
  imports: [IUI],
  templateUrl: './loading.html',
  styleUrl: './loading.scss',
})
export class IRLoading {}
