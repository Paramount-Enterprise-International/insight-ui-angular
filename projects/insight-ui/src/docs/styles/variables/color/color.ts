import { Component } from '@angular/core';
import { IUI } from '@insight/ui';

@Component({
  selector: 'ir-color',
  imports: [IUI],
  templateUrl: './color.html',
  styleUrl: './color.scss',
})
export class IRVariableColor {}
